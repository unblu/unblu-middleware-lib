name: unblu-middleware-lib-quarkus-dialog-bot
artifact: ${projectGroup}:${projectArtifactId}:${projectVersion}
metadata:
  short-name: unblu-dialog-bot
  guide: https://quarkus.io
deployment-artifact: ${projectGroup}:unblu-middleware-lib-quarkus-deployment:${projectVersion}
